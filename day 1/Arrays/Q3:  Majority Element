class Solution {
public:
    int majorityElement(vector<int>& nums) {
        sort(nums.begin(),nums.end());
        int size = nums.size();
        int start = 0;
        int end = size-1;
        
            int mid = nums[start+(end-start)/2];
        return mid;
        
    }
};
